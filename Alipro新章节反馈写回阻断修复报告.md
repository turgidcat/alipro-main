# Alipro新章节反馈写回阻断修复报告

## 1. 问题根因

本轮阻断项的根因不在前端 UI 展示，也不在章节承接参数丢失，而是在后端 `chapter_feedback` 生成结果的 JSON 解析链路过于脆弱。

- 新书第 2 章及后续章节生成后，正文先保存成功。
- 随后的正式反馈生成如果遇到模型返回的 JSON 不够严格、末尾截断、引号异常或尾逗号等情况，会直接触发 `反馈解析失败`。
- 一旦这里直接失败，后续 `chapter_summary`、`next_chapter_focus`、`quality_check` 就不会写回到章节计划结构包里，于是前端出现：
  - `正文已保存，但正式反馈/摘要/质检未写回：反馈解析失败`

已确认这不是“旧书兼容”专属问题，而是会影响新书主链路的真实 P0 阻断项。

## 2. 修改文件

- `backend/routes/ai.js`
- `Alipro新章节反馈写回阻断修复报告.md`

## 3. 是否修改后端

是。

本轮做了最小范围的后端修复，原因是根因明确落在后端 `chapter_feedback` 解析与恢复逻辑，而不是前端字段读取。

## 4. 是否修改生成链路核心逻辑

没有重构生成链路核心逻辑。

本轮没有修改：

- 数据库结构
- 接口字段名
- 前端架构
- AI 生成主链路的章节正文生成机制

只是在原有 `chapter_feedback` 返回解析后，补了最小恢复能力，避免一次非严格 JSON 直接导致整包写回失败。

## 5. 修复方式

本轮修复点如下：

1. 放宽 `tryParseJsonObject(...)` 的容错能力。
   - 补了常见 JSON 异常修复：
   - 智能引号替换
   - `NBSP` 清理
   - 结尾尾逗号清理

2. 在 `handleChapterFeedbackGeneration(...)` 中加入最小恢复流程。
   - 首次解析失败时，先记录告警日志与失败上下文。
   - 如果判断像是模型输出截断或 JSON 未闭合，先低温重试一次原提示。
   - 如果仍失败，再走一个“只修 JSON、不补新事实”的 repair prompt，把失败输出修成严格 schema。

3. 保留成功写回真实性，不把失败伪装成成功。
   - 只有真的拿到可解析反馈包，才继续保存 `chapter_summary`、`next_chapter_focus`、`quality_check`。
   - 没有简单把错误提示隐藏掉。

## 6. 新书 1-3 章验收结果

验收书：

- 书名：`反馈写回验收书-1782144235139`
- 书籍 ID：`39b22240-0557-4601-854b-6b6f37204c80`

验收结果：

1. 第 1 章通过
   - 正文已保存
   - `chapter_summary` 已写回
   - `quality_check` 已写回
   - 数据来源正常：
   - `feedbackSource = model_feedback`
   - `qcSource = model_audit`

2. 第 2 章通过
   - 承接第 1 章生成
   - 正文已保存
   - `chapter_summary` 已写回
   - `quality_check` 已写回
   - 未再出现 `反馈解析失败`

3. 第 3 章通过
   - UI 明确显示 `3 / 3`、`开始第 3 章`
   - 点击生成后按钮进入禁用状态，出现明确“正在生成”反馈
   - 生成完成后页面提示：
   - `正文、摘要和质量检查都已写回，下一章会自动读取这份承接信息。`
   - 数据库核对通过：
   - 第 3 章正文长度 `5935`
   - `feedbackSource = model_feedback`
   - `qcSource = model_audit`

4. 刷新后持久化通过
   - 刷新后工作台重新显示 `1 / 3`
   - 第 1-3 章章节计划仍可读取
   - 数据库核对确认 1-3 章正文都存在，反馈包都存在

数据库复核结果：

- 第 1 章：`summaryLen=72`，`qcStatus=passed`
- 第 2 章：`summaryLen=109`，`qcStatus=warning`
- 第 3 章：`summaryLen=82`，`qcStatus=warning`

## 7. build 是否通过

通过。

执行命令：

```bash
cd frontend-react
npm run build
```

结果：

- Vite production build 成功
- 仅有既有的 chunk size warning
- 无构建阻断

## 8. 是否仍存在一票否决问题

本轮围绕“新章节反馈/摘要/quality_check 写回失败”的 P0 阻断项，当前未再发现一票否决问题。

说明：

- 浏览器控制台未捕获前端业务相关红错
- 第 3 章生成中按钮已禁用，避免重复点击
- 新书 1-3 章正文、摘要、质检均已完成写回
- 未再复现 `正文已保存，但正式反馈/摘要/质检未写回：反馈解析失败`

仍需注意但不构成当前一票否决：

- 第 2、3 章的 `quality_check` 状态为 `warning / needs_review`，属于内容质量提醒，不是写回失败
- 生成内容本身仍可能出现承接质量风险提示，这属于模型内容质量问题，不是本轮写回阻断项

## 9. 是否建议重新进入小范围内测前最终验收

建议。

理由：

- 本轮 P0 阻断项已经定位清楚并完成最小修复
- 新书 1-3 章闭环已重新跑通
- 摘要与 `quality_check` 写回链路已恢复稳定
- build 通过
- 前端业务控制台未见新的红错

建议下一步：

- 在当前分支基于这次修复结果，重新执行一次“小范围内测前最终验收”
- 重点复核新书链路与旧书校改兼容提示，确认没有新的回归
