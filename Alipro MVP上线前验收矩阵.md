# Alipro MVP 上线前验收矩阵

> 当前分支：`codex/workbench-refactor-checkpoint`  
> 当前基线：`fb03ab4 chore: checkpoint before workbench refactor`  
> 本轮原则：先记录验收口径，再以代码审计、脚本验证、浏览器实测和数据库/API 复核更新结论；没有真实证据不写“通过”。

## 验收总览

| 维度 | 验收范围 | 当前状态 | 阻断级别 | 证据来源 |
| --- | --- | --- | --- | --- |
| D1 核心生成链路完整性 | 新书创建、章节计划、正文生成、保存、反馈生成、刷新后查看 | 待验证 | P0 关注 | 代码审计 + 新书 1-3 章实测 |
| D2 生成控制项有效性 | 字数、题材/平台/模板、章节细纲、角色、剧情线等是否进入 payload/prompt | 待验证 | P1 关注 | 前后端字段映射审计 + prompt 证据 |
| D3 生成结果质量与结构化写回 | 正文、摘要、feedback、quality_check、下一章承接信息 | 待验证 | P0 关注 | API/数据库复核 + 页面展示 |
| D4 剧情线机制有效性 | 剧情线选择、prompt 约束、正文体现、进度写回、状态更新、前端推进结果、专项质检 | 待验证 | P0/P1 关注 | 代码审计 + 样本正文核对 + 前端展示复核 |
| D5 生成过程体验与状态可见性 | loading、实时正文片段、成功/失败提示、保存反馈 | 待验证 | P1 关注 | 浏览器实测 + 前端状态审计 |
| D6 创作台信息架构与正文工作体验 | 编辑器式工作台、正文预览、质量检查/反馈/下一章关注可见 | 待验证 | P1 关注 | 浏览器实测 + UI 审计 |
| B1 上线基础检查 | build、控制台、安全、接口、旧书兼容、工作区状态 | 待验证 | P0/P1 | build + 控制台 + rg + git diff |

## 已知问题跟踪

| 编号 | 级别 | 问题 | 当前处理口径 |
| --- | --- | --- | --- |
| P0-1 | P0 | 新书第 2 章曾出现“正文已保存，但正式反馈/摘要/质检未写回：反馈解析失败” | 必须复验真实写回，不能只看 UI 成功提示 |
| P1-1 | P1 | 流式生成可能没有实时展示正文片段 | 必须确认是否实时显示；若没有，记录为残留或做最小修复 |
| P1-2 | P1 | 目标字数可能严重失控 | 必须记录 1-3 章目标字数、实际字数、偏差比例 |
| P1-3 | P1 | 剧情线机制是否真实控制正文尚未验证 | 已修复并复验：prompt、usedStorylineIds、usedBeatIds、storyline_audit、storyline 写回均通过 |
| P2-1 | P2 | 全 AI 自动链路尚未完整验收 | 完整 AI 托管模式明确后置，不作为辅助创作模式上线前阻断项 |
| P1-5 | P1 | 主要前端生成控制项是否进入生成链路尚未验证 | 必须做字段映射审计 |

## 最终判断口径

只有同时满足以下条件，才建议进入小范围内测：

1. P0 清零。
2. 新书 1-3 章闭环通过。
3. 正文、摘要、feedback、quality_check 稳定写回。
4. 第 2 / 3 章能承接前文。
5. 主要生成控制项至少不是假控制。
6. 目标字数没有严重失控，或有明确提示和降级策略。
7. 流式生成状态对用户可见，或明确标记为 P1 残留并说明不阻断的理由。
8. 辅助创作模式的剧情线机制完成最小闭环：生成前可选择/调度，生成中进入 prompt，生成后写回进度，前端可见推进结果，summary / quality_check 能检查推进情况。
9. build 通过。
10. 控制台无前端业务红错。
11. 无前端硬编码密钥。
12. 没有越界大改。

## 本轮最终结果快照

| 维度 | 最终结论 | 关键证据 |
| --- | --- | --- |
| D1 核心生成链路完整性 | 辅助创作链路通过；全 AI 托管为 P2 规划项 | 复验书 `c598512f-fd38-4a00-b54d-53a074890bd3` 第 1-3 章正文、保存、反馈链路闭环；inspect 三章 `status=ok`；当前未发现完整全 AI 托管入口 |
| D2 生成控制项有效性 | 基本通过，字数 P1 观察；全 AI 策略控制为 P2 | 章节细纲、角色、剧情线、目标字数进入 payload/prompt；字数控制从最高 +69.6% 收敛到快速复验 +27.4%，仍需观察；未发现全 AI 严格度/批次/自动调整权限入口 |
| D3 生成结果质量与结构化写回 | 辅助创作写回通过；全 AI 写回未形成独立模式 | 第 1-3 章均有 `model_feedback`、摘要、`quality_check.source=model_audit`；刷新/API 读回均存在；未发现写回失败仍自动继续生成 |
| D4 剧情线机制有效性 | 通过；辅助创作模式完整剧情线闭环已完成 | generate / inspect 三章 `status=ok`；`usedStorylineIds` 与稳定 `usedBeatIds` 均有记录；`storylineAuditStatus=advanced`；`storylineWritebackOk=true`；前端可见剧情线推进结果 |
| D5 生成过程体验与状态可见性 | 通过；主入口流式正文实时展示已接入，全 AI 批次状态为 P2 | 主入口优先 `/api/generate/stream`；实测 SSE `event: delta`；同步生成保留为早期失败 fallback；未发现全 AI 批次进度/批次报告入口 |
| D6 创作台信息架构与正文工作体验 | 基础可用通过，信息架构优化后置 | 浏览器可见创作台、章节配置、生成入口、QUALITY CHECK、STORYLINE PROGRESS、FEEDBACK、NEXT FOCUS；双模式入口尚未形成完整产品表达；页面信息架构、卡片密度、正文阅读空间、正文聚焦模式仍属后续 P2 优化项 |
| B1 上线基础检查 | 通过，脚本白名单 WARN | build 通过；健康检查 200；旧书保存抽样通过；无前端硬编码真实密钥；`check-mvp-rescue.ps1` 可运行但旧白名单对新报告给 WARN |

## 复验样本

- 三章复验书：`MVP矩阵复验书-1782149074878`，bookId=`c598512f-fd38-4a00-b54d-53a074890bd3`
- 字数快速复验书：bookId=`393135b8-2452-48fb-ab71-858be79516f7`
- 旧书抽样：`破雾修真录`，bookId=`b8bd76b6-170f-4666-9233-36e86b1f8b1d`

## 双模式创作机制纳入 D1-D6

| 维度 | 当前判断 | P 级别 | 上线前处理 |
| --- | --- | --- | --- |
| D1 | 当前验证通过的是辅助创作链路；未发现完整全 AI 托管入口。 | P2 | 不作为本轮阻断项，但不能写成已支持完整托管。 |
| D2 | 没有全 AI 严格度、批次、剧情线自动调整权限等真实控制项。 | P2 | 作为后续模式设计项记录。 |
| D3 | 辅助模式写回已通过；未形成全 AI 模式下“写回成功才继续下一章”的独立验收对象。 | P2 | 当前不伪装为全 AI 闭环。 |
| D4 | 全 AI 自动剧情线调度未实现。 | P2 | 后续设计；辅助创作模式剧情线闭环已完成。 |
| D5 | 没有全 AI 批次进度、风险中断、批次报告。 | P2 | 后续体验能力。 |
| D6 | UI 没有完整双模式入口；当前以辅助创作工作台为主。 | P2 | 后续要明确模式入口与说明。 |

## 剧情线机制纳入 D1-D6

| 维度 | 当前判断 | P 级别 | 上线前处理 |
| --- | --- | --- | --- |
| D1 | 卷纲/章节计划/剧情线/正文链路的基础路径已成立，剧情线不阻断连续生成。 | 已修 | 已完成最小修复。 |
| D2 | 主推/关联剧情线进入 prompt 与 `storyline_context`；无结构化 beat 时生成稳定 fallback beat id。 | 已修 | 已形成“本章主推 / 关联 / 当前 beat / 必须推进 / 禁止提前发生”的最小生成前规则。 |
| D3 | `storyline_context` 与反馈写回链路未导致 summary / feedback / quality_check 失败；剧情线推进结果已写入 `storyline_progress` 与 `quality_check.storyline_audit`。 | 已修 | 三章 inspect 确认 feedback、summary、quality_check、storyline 写回均存在。 |
| D4 | 基础剧情线有效；状态、推进结果展示、剧情线专项质检形成最小闭环。 | 已修 | 不再归为 P2。 |
| D5 | 当前章主推线、挂载状态、推进结果、used beat、复核原因可见。 | 已修 / 全 AI P2 | 辅助创作模式完成；全 AI 批次联动仍 P2。 |
| D6 | 剧情线 UI 基础可操作，正文区和抽屉能展示推进摘要、状态和 used beat。 | 已修 / 高级体验 P2 | 最小推进结果可见已完成；高级时间轴和视觉体验后置。 |
