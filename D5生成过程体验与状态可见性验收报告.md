# D5 生成过程体验与状态可见性验收报告

## 结论

部分通过，流式正文实时展示保留 P1。

## 已通过

- 页面可见生成入口。
- 生成过程中按钮/状态会进入 loading。
- 生成后页面/API 能看到正文、质量检查、反馈、下一章关注。
- 保存成功与部分完成状态不会被伪装成完全成功；前端状态文案仍区分“正文已保存但反馈未写回”的情况。

## 未完全通过

当前主工作台入口 `App.jsx` 仍使用非流式 `generateChapterContent(...)` 调用 `/api/generate`。仓库中存在 `streamChapterContent(...)` 和流式草稿恢复相关代码，但当前主入口未实际实时渲染正文 chunk。

## 风险判断

这不是 P0，因为：

- 用户生成期间有 loading 状态。
- 生成完成后保存前/保存后正文可见。
- 写回失败不会被隐藏。

但它仍是 P1：后续应补齐主入口的流式正文展示，或把“非实时生成”明确作为产品状态。
