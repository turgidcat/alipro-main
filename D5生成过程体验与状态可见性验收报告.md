# D5 生成过程体验与状态可见性验收报告

## 结论

通过，流式正文实时展示已接入主入口。

## 已通过

- 页面可见生成入口。
- 生成过程中按钮/状态会进入 loading。
- 生成后页面/API 能看到正文、质量检查、反馈、下一章关注。
- 保存成功与部分完成状态不会被伪装成完全成功；前端状态文案仍区分“正文已保存但反馈未写回”的情况。

## 未完全通过

当前主工作台入口 `App.jsx` 已优先使用 `streamChapterContent(...)` 调用 `/api/generate/stream`。生成过程中正文 chunk 会实时追加到正文预览和校改草稿；如果流式在未收到正文前失败，会回退原同步 `generateChapterContent(...)`。

## 风险判断

这不是 P0，因为：

- 用户生成期间有 loading 状态。
- 生成完成后保存前/保存后正文可见。
- 写回失败不会被隐藏。

该项已修复。实测 `/api/generate/stream` 返回 `text/event-stream`，并在完成前收到 `event: delta`。

## 双模式创作机制归入 D5

辅助创作模式下，单章生成 loading、成功、部分完成、错误状态可见。当前未发现完整全 AI 托管入口，因此也没有批次生成进度、风险中断原因、批次报告等全 AI 状态面板。

这属于 P2 后续能力；如果未来 UI 出现“全 AI 托管”入口，就必须同步验收批次进度和中断原因，否则会升级为 P1。

## 剧情线机制归入 D5

当前章主推线、挂载状态、生成前建议在 UI 中可见。

本轮已让用户看到剧情线推进结果：正文工作区新增 `STORYLINE PROGRESS`，展示本章实际使用了哪些剧情线 / beat、写回了什么推进摘要、是否需要复核；剧情线抽屉展示状态、最近推进章节、推进摘要和 used beat。

全 AI 模式下的批次进度、自动中断原因、批次报告可以继续归为 P2；辅助创作模式下的剧情线推进结果可见性已完成。
