# 施工看板

> 更新时间：2026-06-20
> 用途：日常开工时快速看，不替代完整任务清单。
> 完整版请看：[最新任务状态.md](/C:/Users/turgidcat/Desktop/alipro-main/docs/最新任务状态.md)
> 分类导航请看：[文档总索引.md](/C:/Users/turgidcat/Desktop/alipro-main/docs/文档总索引.md)

## 当前总判断

- 主链业务侧的 `P0/P1` 仍然成立，前端整体改造本轮已完成核心收口。
- 前端重构当前真实状态更新为：`P0/P1/P2` 已完成，`P3` 的主要视觉交付已并入本轮结果验收。
- 当前不再是“继续拆骨架”，而是进入“稳定使用 + 前端细修 + 生成链路校正”的阶段。
- 旧路线图文档只保留为阶段设计归档，不再作为当前施工顺序依据。

## 2026-06-19 增量校准

- 已完成：
  - `App.jsx` 继续瘦身到 `346` 行
  - `RevisionEditor` / `GenerationBanner` / `GenerationSettingsPopover` 已独立成组件
  - 章节列表已切到真实三态，不再使用占位算法
  - `Ctrl+Enter`、`Esc`、`[`、`]` 快捷键已接通
  - 抽屉记忆与应用内未保存拦截已接通
  - 校改弹窗已恢复“建议查看 + 最终稿编辑 + 保存回写”闭环
  - `frontend-react` 构建已通过
- 非阻塞观察：
  - Vite 构建存在单 chunk 超过 `500 kB` 的提示，属于后续性能优化项，不影响当前交付使用

## 前端重构专项

### 前端 P0【主体完成】

- 已完成:
  - `react-router-dom` 接管
  - Tailwind 接入
  - `App.jsx` 主要纯函数 / 状态 / 展示组件拆分
  - `WorkbenchPage.jsx` 死代码删除
  - `App.jsx` 已进一步压缩到 `346` 行

### 前端 P1【已完成】

- 已完成:
  - 创作台从 3 Tab 改为双栏 + 抽屉
  - 顶部全局条、章节配置区、正文工作区、4 个抽屉全部可见
  - `WorkflowTabs` / `activeStep` 已移除
  - 章节列表真实三态已接通
  - 页面已达到当前分支交付视觉状态

### 前端 P2【已完成】

- 已完成重点:
  - 章节列表真实三态
  - `RevisionEditor` / `GenerationBanner` / `GenerationSettingsPopover` 拆分
  - 快捷键、抽屉记忆、未保存提示
  - 参数折叠和次级入口收口

## 当前阶段

### P0【已完成】

- 已真实验通：
  - 章节任务表
  - 正文生成
  - `chapter_feedback` 回写
  - 校改保存
  - 前端编排下的章节结果落库
- 已确认：
  - `POST /api/generate` 负责返回正文结果，本身不直接写 `chapters` 表
  - `/workbench` 会继续调用章节落库接口与反馈回写链，完成“生成后可继续写回”的完整流程
  - `chapter_feedback.updated_at` 会真实刷新
  - `/workbench` 上生成后和校改后，反馈都会同步更新
  - 回归样本已恢复，不留脏测试数据

### P1【已完成】

- 已完成入口收口：
  - React 工作台已是主入口
  - `chapter_plans` 已是章节规划主对象
  - 生成链路说明已切到新的中文文档体系维护
- 已完成样例 / 批量收口：
  - 今天重新复验批量生成 `1-5` 章全部通过
  - 第 `1-4` 章走 `outline_text`
  - 第 `5` 章走 `stored_structured_outline`
  - 本轮字数浮动均在 `10%` 内
- 已完成说明收口：
  - 不再把“批量链路已验真”和“演示书已完全补齐”混成一句话
  - 《破雾修真录》当前可确认是 `1-8` 章任务表连续挂在同一主线下
  - 其中前段章节仍含短正文种子，不假装它们都已经是完整长正文样板

### P2【进行中】

- `book_plans` / `volume_plans` 已开始成为正式主表
- 剧情线进度已能从章节反馈写回 `storylines.structured_content.chapter_progress`
- `chapter_feedback` 仍挂在 `structured_content`，还没正式独立
- 最新增量校正：
  - 章节细纲前端已改成“单一最新稿”
  - 系统生成与手动编辑前端合并显示，不再双轨暴露
- 旧兼容层还没退场，当前重点是压缩旧职责，不是继续扩样例

## 已经别再重复确认的事项

- React 主工作台已经接管创作主链
- `chapter_plans` 已经是章节规划主对象
- 主链完整闭环已经跑通过
- 批量生成不是只写在页面上的描述，今天已重新复验通过

## 当前卡点

### 结构层

- `chapter_plans` 里仍同时挂着目标层和执行层
- `storylines` 已接通回写，但还没成为稳定中枢
- `chapter_feedback` 还没独立成正式层

### 兼容层

- `novel_outlines` / `volume_settings` 仍在兼容保留
- 旧静态入口还没到正式下线时机

### 数据样板

- 《破雾修真录》当前同时存在任务表样板和短正文种子
- 如果后面继续补演示章节，必须明确标注“任务表已补”还是“完整正文已补”

## 下一步建议顺序

1. 进入稳定期使用，优先按真实创作流程做回归，而不是继续大拆。
2. 如需继续优化，优先做前端性能项，例如 chunk 拆分和低频面板懒加载。
3. 旧兼容层和演示样板后续单独收口，不再和本轮前端重构混写。
