# 施工看板

> 更新时间：2026-06-18
> 用途：日常开工时快速看，不替代完整任务清单。
> 完整版请看：[latest-task-status.md](/C:/Users/turgidcat/Desktop/alipro-main/docs/latest-task-status.md)

## 当前总判断

- 主链业务侧的 `P0/P1` 仍然成立，但前端整体改造也出现了自己的 `P0/P1/P2/P3` 分期。
- 前端重构当前真实状态是：`P0` 主体完成、`P1` 主体完成但未完全验收、`P2/P3` 待实施。
- 当前真正要做的，不是重讲方向，而是把前端改造文档和真实落地状态对齐，然后继续推进 `P2`。

## 前端重构专项

### 前端 P0【主体完成】

- 已完成:
  - `react-router-dom` 接管
  - Tailwind 接入
  - `App.jsx` 主要纯函数 / 状态 / 展示组件拆分
  - `WorkbenchPage.jsx` 死代码删除
- 未完全收口:
  - `App.jsx` 仍约 `1347` 行,没有继续压到计划中的更理想体量

### 前端 P1【主体完成,验收未完】

- 已完成:
  - 创作台从 3 Tab 改为双栏 + 抽屉
  - 顶部全局条、章节配置区、正文工作区、4 个抽屉全部可见
  - `WorkflowTabs` / `activeStep` 已移除
- 未完成:
  - 章节列表三态仍是占位算法
  - 页面视觉仍是骨架版,不能按最终审美验收

### 前端 P2【未开始】

- 待做重点:
  - 章节列表真实三态
  - `RevisionEditor` / `GenerationBanner` 拆分
  - 快捷键、抽屉记忆、未保存提示
  - 参数折叠和“更多”次级入口

## 当前阶段

### P0【已完成】

- 已真实验通：
  - 章节任务表
  - 正文生成
  - `chapter_feedback` 回写
  - 校改保存
  - 章节结果落库
- 已确认：
  - `chapter_feedback.updated_at` 会真实刷新
  - `/workbench` 上生成后和校改后，反馈都会同步更新
  - 回归样本已恢复，不留脏测试数据

### P1【已完成】

- 已完成入口收口：
  - React 工作台已是主入口
  - `chapter_plans` 已是章节规划主对象
  - `generation-logic` 已按新链路重写说明
- 已完成样例 / 批量收口：
  - 今天重新复验批量生成 `1-5` 章全部通过
  - 第 `1-4` 章走 `outline_text`
  - 第 `5` 章走 `stored_structured_outline`
  - 本轮字数浮动均在 `10%` 内
- 已完成说明收口：
  - 不再把“批量链路已验真”和“演示书已完全补齐”混成一句话
  - 《破雾修真录》当前可确认是 `1-8` 章任务表连续挂在同一主线下
  - 其中前段章节仍含短正文种子，不假装它们都已经是完整长正文样板

### P2【进行中】

- `book_plans` / `volume_plans` 已开始成为正式主表
- 剧情线进度已能从章节反馈写回 `storylines.structured_content.chapter_progress`
- `chapter_feedback` 仍挂在 `structured_content`，还没正式独立
- 旧兼容层还没退场，当前重点是压缩旧职责，不是继续扩样例

## 已经别再重复确认的事项

- React 主工作台已经接管创作主链
- `chapter_plans` 已经是章节规划主对象
- 主链完整闭环已经跑通过
- 批量生成不是只写在页面上的描述，今天已重新复验通过

## 当前卡点

### 结构层

- `chapter_plans` 里仍同时挂着目标层和执行层
- `storylines` 已接通回写，但还没成为稳定中枢
- `chapter_feedback` 还没独立成正式层

### 兼容层

- `novel_outlines` / `volume_settings` 仍在兼容保留
- 旧静态入口还没到正式下线时机

### 数据样板

- 《破雾修真录》当前同时存在任务表样板和短正文种子
- 如果后面继续补演示章节，必须明确标注“任务表已补”还是“完整正文已补”

## 下一步建议顺序

1. 继续推进 `P2`，把结构层职责拆清。
2. 压缩旧兼容层职责，准备旧入口退场条件。
3. 如需继续补演示章节，单独记新增施工，不把样板误写成完工。
