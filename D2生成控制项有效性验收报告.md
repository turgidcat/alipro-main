# D2 生成控制项有效性验收报告

## 结论

基本通过，目标字数仍保留 P1 观察项。

## 字段链路

| 控制项 | 前端/API payload | 后端使用 | 结论 |
| --- | --- | --- | --- |
| 章节细纲 | `outline`、`chapterStructure`、`chapterPlan.structured_content.chapter_outline_structure` | `finalOutline` 与 prompt `章节任务表` | 有效 |
| 目标字数 | `wordCount`、`generation_settings.word_count` | `buildCreativePrompt(...wordCount)` 与 `maxTokens` | 有效但需观察 |
| 题材/子题材 | `genre`、`subgenre` | prompt 类型描述 | 有效 |
| 平台/模板 | `platform`、`template` | 平台风格与写法模板 | 有效 |
| 角色 | `appearing_roles`、`role_execution`、`character_notes` | prompt 角色硬约束与角色执行参数 | 有效 |
| 剧情线 | `main_storyline_id`、`target_storylines` | 修复后进入 `storyline_context` 与 prompt | 有效 |

## 字数控制证据

三章复验中，3000 目标下实际长度为：

| 章节 | 目标 | 实际 | 偏差 |
| --- | ---: | ---: | ---: |
| 第 1 章 | 3000 | 3971 | +32.4% |
| 第 2 章 | 3000 | 5089 | +69.6% |
| 第 3 章 | 3000 | 4287 | +42.9% |

针对该问题，本轮最小修复：

- 将正文生成 `maxTokens` 从 `wordCount * 2.0` 收紧为 `wordCount * 1.35`。

修复后快速复验：

| 目标 | 实际 | 偏差 | finishReason | maxTokens |
| ---: | ---: | ---: | --- | ---: |
| 3000 | 3822 | +27.4% | `stop` | 4051 |

## 残留

目标字数仍不是精确硬约束，建议在小范围内测中继续观察；当前已从严重飘字收敛为可提示/可观察的 P1。

## 双模式创作机制归入 D2

当前没有发现“辅助创作 / 全 AI 托管”的正式模式选择控件，也没有全 AI 严格度、自动生成批次、剧情线自动调整权限等真实控制项。因此：

- 辅助创作模式下的章节细纲、角色、剧情线、目标字数等控制项基本有效。
- 全 AI 托管相关控制项不存在，不能写成已完成。
- 该缺口归为 P2，前提是 UI / 报告不暗示当前已经支持完整全 AI 托管。

## 剧情线机制归入 D2

主推剧情线和关联剧情线已进入 prompt / `storyline_context`，属于已修复的基础能力。

本轮已补齐辅助创作模式的最小明确调度规则：本章主推、关联线、当前 beat、必须推进、禁止提前发生、伏笔埋设/回收边界会进入生成前约束；无结构化 beat 时生成稳定 fallback beat id，避免 `usedBeatIds` 为空。

完整 AI 托管下的自动调整权限、批次策略和智能重排可以 P2；辅助创作模式的最小调度规则已完成。
