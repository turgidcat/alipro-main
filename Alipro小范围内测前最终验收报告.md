# Alipro 小范围内测前最终验收报告

## 1. 新书 1-3 章流程是否通过

- 结论：**未通过**
- 本次实际验收到的新书样本：`内测验收书-1782142040430`
- 已确认通过的步骤：
  - 创建新测试书成功
  - 创建表单保存成功，可视为基础设定已保存
  - 第 1 章角色设定可保存，且有明确提示 `本章角色已保存`
  - 第 1 章任务表可保存，且有明确提示 `章节任务表已保存`
  - 第 1 章生成时按钮会变成 `正在生成...` 且禁用，生成完成后可看到正文查看入口、`QUALITY CHECK`、`FEEDBACK`、`NEXT FOCUS`
  - 第 2 章可以创建并进入可生成状态，生成中禁用重复点击也正常
  - 第 2 章内容能明显承接第 1 章：正文与反馈中出现了第 1 章留下的名片编号、旧案编号、周宁/许照、城郊封闭园区等承接信息
- 阻断点：
  - 第 2 章生成后出现明确提示：`正文已保存，但正式反馈/摘要/质检未写回：反馈解析失败`
  - 这意味着第 2 章不是完整成功，而是“正文保存成功、反馈/摘要/质检写回失败”的部分完成态
  - 因为第 2 章已经在新书主链路上失败，所以本次未继续将其判定为“1-3 章流程通过”，第 3 章与刷新后 3 章完整可查看链路也**不能算验收通过**

## 2. 旧书保存校改是否存在风险

- 结论：**存在风险，但风险判断已变化**
- 本次抽样旧书：`破雾修真录-MVP验收`
- 在这本旧书上执行“查看/校改正文”后保存校改：
  - 未复现 `正文已保存，但正式反馈/摘要/质检未写回`
  - 未复现 `反馈解析失败`
  - 页面仍可看到 `QUALITY CHECK` 区块，且显示 `质量检查通过`
- 但本次更重要的发现是：
  - `反馈解析失败` 已经在**新建测试书的第 2 章**出现
  - 所以它**不再只是旧数据兼容风险**
  - 它已经影响到新章节主链路，不能继续按“只影响旧书”的结论处理

## 3. 控制台是否有红错

- 浏览器控制台未发现前端业务相关红色错误
- `tab.dev.logs` 抽查为空
- 验收过程中出现的 `Statsig` 超时日志来自浏览器插件侧，不属于 Alipro 前端业务控制台错误，不计入项目业务红错

## 4. build 是否通过

- 通过
- 执行命令：`cd frontend-react && npm run build`
- 结果：Vite 构建成功
- 备注：仍有既存的 chunk size warning，但不是本次阻断项

## 5. 是否存在一票否决问题

- **存在**
- 一票否决问题：
  - 新书主链路在第 2 章就出现 `正文已保存，但正式反馈/摘要/质检未写回：反馈解析失败`
  - 这直接影响了新章节生成后的完整反馈闭环
  - 也使 `quality_check` 在新章链路上不能稳定完整写回
- 因此当前分支**不应**判定为可进入小范围上线 / 内测准备

## 6. 是否建议进入小范围内测

- **不建议**

## 7. 如果不建议，最小阻断项

- 修复新书主链路中第 2 章及后续章节生成后的反馈解析/写回问题，至少要确保：
  - 正文生成成功后，摘要能够稳定写回
  - `quality_check` 能够稳定写回并独立展示
  - 不再出现 `反馈解析失败`
- 在修复后重新跑一遍新书闭环：
  - 创建新书
  - 第 1 章生成并保存
  - 第 2 章承接前文并生成成功
  - 第 3 章承接前文并生成成功
  - 刷新后 1-3 章仍可查看
