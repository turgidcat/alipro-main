# D4 剧情线机制有效性验收报告

## 结论

通过。辅助创作模式下的最小完整剧情线闭环已达上线前标准；完整 AI 托管模式仍后置为 P2。

## 发现的问题

第一次三章复验时，正文内容能体现剧情线关键词，但生成 metadata 显示：

- `storylineConstraintApplied=false`
- `usedStorylineIds=[]`

原因：正文生成阶段只读取已保存的 `storyline_context` 快照；新建章节尚未保存快照时，实时计算出的 `chapterStorylineContext` 没有进入生成约束。

## 本轮修复

修改 `backend/routes/ai.js`：

- 优先使用已保存 `storyline_context`。
- 如果没有快照，使用本次实时计算的 `chapterStorylineContext` 转换为持久化约束。
- 约束有效时同步写回章节计划快照。

## 修复后证据

复验书 `c598512f-fd38-4a00-b54d-53a074890bd3`：

| 章节 | storylineConstraintApplied | usedStorylineIds | 正文关键词 |
| --- | --- | --- | --- |
| 第 1 章 | true | 非空 | 橙灯密约、白塔钥匙 |
| 第 2 章 | true | 非空 | 橙灯密约、灰鸦账本、白塔钥匙 |
| 第 3 章 | true | 非空 | 橙灯密约、灰鸦账本、白塔钥匙 |

第 2 / 3 章均能承接前文人物与物证链。

## 重新审计后的真实完成度与本轮修复

当前版本已经具备基础剧情线能力：

- 可以创建/挂载剧情线。
- 可以指定本章主推剧情线。
- 可以把剧情线约束带入正文生成。
- 可以把 `storyline_context` 写入章节计划。
- 生成结果能看到剧情线影响。
- 数据库已有 `storylines.status` 字段，AI 生成剧情线大纲后会写为 `generated`；章节反馈回写时会尝试把 `draft` 推进为 `active`。
- 章节生成 metadata 会返回 `usedStorylineIds` / `usedBeatIds`；无结构化 beat 时会生成稳定 fallback beat id。
- 章节反馈写回时会把 `chapter_progress`、`last_chapter_feedback`、`currentProgress` 写入 `storylines.structured_content`。
- `quality_check.storyline_audit` 会记录剧情线推进状态、usedStorylineIds、usedBeatIds、风险和是否需要复核。
- 前端正文工作区新增 `STORYLINE PROGRESS`，剧情线抽屉可见状态、最近推进章节、推进摘要和 used beat。

本轮二次修复后，辅助创作模式最小闭环已经成立：

- 生成前：章节计划可选择主推 / 关联剧情线，并生成当前章节剧情线约束。
- 生成中：剧情线约束进入 prompt，旧 `storyline_context` 缺 beat 时会刷新为实时约束。
- 生成后：feedback 写回 `storyline_progress`，`quality_check` 写回 `storyline_audit`。
- 剧情线侧：`storylines.structured_content.chapter_progress/currentProgress` 可读回每章推进结果。
- 前端侧：正文工作区和剧情线抽屉可见使用了哪些剧情线、beat、推进摘要和状态。

因此：完整 AI 托管模式下的自动调度、预授权、批次中断可以归为 P2；辅助创作模式下“剧情线进度可见、推进结果可验、状态可更新、质检可识别”的最小闭环已完成。

## 本轮通过证据

- `node verify-batch-generation.js --mode generate --base-url http://127.0.0.1:3000 --book-id c598512f-fd38-4a00-b54d-53a074890bd3 --chapters 1,2,3`：三章 `status=ok`。
- `node verify-batch-generation.js --mode inspect --book-id c598512f-fd38-4a00-b54d-53a074890bd3 --chapters 1,2,3`：三章 `status=ok`。
- 第 1-3 章均记录 `usedStorylineIds=5d394565-32cb-42e8-b993-d129ef431a66`。
- 第 1-3 章均记录稳定 `usedBeatIds=fallback:5d394565-32cb-42e8-b993-d129ef431a66:chapter:N`。
- 第 1-3 章 `storylineAuditStatus=advanced`，`storylineWritebackOk=true`。

## 可后置项

- 完整全 AI 托管模式。
- 自动批次生成、预授权策略、风险中断和批次报告。
- 更复杂的动态依赖评分、跨卷自动重排、智能推荐主推线。
