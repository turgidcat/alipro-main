# config

项目级配置目录。

## 当前定位

这里放置跨前后端共享、部署或运行环境相关的配置文件。它不承载业务代码，也不保存运行时数据。

## 结构说明

- 配置文件应保持可读、可审查。
- 不要在这里提交密钥、账号、Token、真实 `.env` 内容。
- 如果配置只服务于某个子系统，优先放到对应子系统目录，例如 `backend/config/`。

## 清理规则

过时配置、个人本地配置、导出配置和临时测试配置不应长期保留在本目录。
